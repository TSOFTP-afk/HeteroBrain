"""
VITA 全局配置
Vita = Life（拉丁语：生命）

设计理念：一个由硅基机械结构中逐渐诞生的数字生命。
"""

# ============ 渐变颜色（RGB） ============
# 顶部：绿色 —— 生命萌发（必须少，只占 ~10%）
GREEN = (50, 255, 100)

# 中间：白色 —— 数据融合 / 意识形成（~20%）
WHITE = (230, 230, 230)

# 底部：灰色 —— 硅基机械（~70%，细分为从深到浅的渐变）
GRAY_DEEP   = (90, 90, 90)     # 最深：纯机械
GRAY_MID    = (120, 120, 120)
GRAY_LIGHT  = (150, 150, 150)  # 最浅：贴近白色过渡

# ============ 启动阶段颜色 ============
STAGE_BOOT = GRAY_MID    # Stage 1 机械启动（灰）
STAGE_DATA = WHITE       # Stage 2 数据形成（白）
STAGE_LIFE = GREEN       # Stage 3 生命出现（绿）

# ============ 动画参数 ============
TYPE_DELAY      = 0.02   # 打字速度（秒/字符）
BAR_DURATION    = 1.0    # 进度条动画时长（秒）
BAR_WIDTH       = 20     # 进度条宽度（字符）
BAR_FRAMES      = 120    # 进度条动画帧数（标准 120 帧）
BLINK_TIMES     = 3      # 光标闪烁次数
