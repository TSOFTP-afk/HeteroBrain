#include <cuda_runtime.h>
#include <cstdio>

#define N_SENSORY_NEURONS     2000

#error "C++ standard is: " __cplusplus

enum class NeuronType : unsigned char {
    EXCITATORY = 0,
};

int main() { return 0; }
